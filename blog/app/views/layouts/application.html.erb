<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title><%= full_title(yield(:title)) %></title>

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css?family=EB+Garamond|Source+Sans+Pro:300,400,600,700" rel="stylesheet">

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if logged_in? %>
      <div class="signed-in-notice">
        <span class="emoji">🌟</span>
        <span>Admin online: <%= @current_user.email %></span> <br>
        <%= link_to "Log Out", logout_path, method: :delete, class: "link" %>
      </div>
    <% end %>

    <%= render 'layouts/nav' unless @disable_nav %>

    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% elsif alert %>
      <p class="alert"><%= alert %></p>
    <% end %>

    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
